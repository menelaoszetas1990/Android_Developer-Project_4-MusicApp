<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- style of the activity label texts -->
    <style name="activityLabelText">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/labelTextSize</item>
        <item name="android:layout_margin">@dimen/labelTextMargin</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- style of the activity implementation texts -->
    <style name="activityImplementationText">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/pointTextSize</item>
        <item name="android:layout_margin">@dimen/pointTextMargin</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- style of buttons -->
    <style name="buttons">
        <item name="android:background">@color/buttonColorAM</item>
        <item name="android:layout_margin">@dimen/pointTextMargin</item>
    </style>

</resources>
